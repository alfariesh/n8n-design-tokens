{
  "name": "design-tokens",
  "version": "1.0.0",
  "description": "Design tokens with Style Dictionary",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "style-dictionary build --watch"
  },
  "devDependencies": {
    "style-dictionary": "^5.1.1"
  }
}
